[build-system]
requires = ["poetry-core>=2.0"]
build-backend = "poetry.core.masonry.api"

[project]
name = "ewma-optimizer"
version = "1.0.0"
description = "Python bindings for the EWMA ARL optimizer (Monte-Carlo simulation)"
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"
dependencies = []
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Programming Language :: Python :: 3",
    "Programming Language :: C++",
]

[project.scripts]
ewma = "ewma.__main__:main"

[tool.poetry]
packages = [{ include = "ewma", from = "python" }]
